# AI风格生成器环境变量配置示例
# 复制此文件并重命名为 .env 或 .env.local 来使用

# 火山方舟API密钥
# 注意：以NEXT_PUBLIC_开头的变量可以在客户端代码中访问
NEXT_PUBLIC_DEFAULT_API_KEY=your-volc-ark-api-key-here

# 其他可选环境变量（如果需要）
# 启用debug模式（可选，默认：false）
# NEXT_PUBLIC_DEBUG=true

# Debug日志级别（可选，值：debug, info, warn, error，默认：info）
# NEXT_PUBLIC_DEBUG_LOG_LEVEL=debug

# 其他可选配置
# NEXT_PUBLIC_API_BASE_URL=https://your-custom-api-url.com